<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Document</title>
</head>
<body>
    <%- include("main") %>

    <main>
        <form action="/new" method="post">
            <h1>Add new book</h1>
            <div>
                <label for="title">Title</label>
                <input type="text" id="title" name="title" value="<%= locals.title  || '' %>">
            </div>

            <div>
                <label for="author">Author</label>
                <input type="text" id="author" name="author" value="<%= locals.author || "" %>">
            </div>

            <div>
                <label for="description">Description</label>
                <input type="text" id="description" name="description" value="<%= locals.description || "" %>">
            </div>

            <div>
                <label for="pages">Pages</label>
                <input type="number" id="pages" name="pages" min="0" value="<%= locals.pages || "" %>">
            </div>

            <button type="submit">Add Book</button>

            <% if (locals.error) { %>
                <% if (locals.error.length > 0) { %>
                    <div class="errors">
                        <ul>
                            <% error.forEach(er => { %>
                                <li><%= er.msg %> your input was <%= er.value || "an empty string" %></li>
                            <% }) %>
                        </ul>
                    </div>
                <% } %>
            <% } %>
        </form>
    </main>
    
</body>
</html>